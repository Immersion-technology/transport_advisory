# Leave blank in dev — Vite proxies /api to localhost:5000 automatically.
# In production, set this to your backend's full URL (no trailing /api).
# Example: VITE_API_URL=https://transport-advisory-backend.onrender.com
VITE_API_URL=
